# Remaining 3v3 Bots Coding Plan

Status: accepted
Source: direct request to decouple the bot implementation from the prior combined shotgun/bots plan; B1-B6 scope from [`3v3-phase2-shotgun-and-bots-planning-helper.md`](3v3-phase2-shotgun-and-bots-planning-helper.md), with gameplay rules from [`3v3-bots-combat-design-handoff.md`](3v3-bots-combat-design-handoff.md)
Run ID: direct
Plan ID: direct
Attempt ID: direct
Covered Requirements: direct request
Baseline: c275c092ff0a042241034a062fe5a4beb389a4a4
Dependencies: None

## Objective

Complete the five-bot 3v3 vertical slice: deterministic difficulty/rule logic, programmatic control, multi-level navigation, bounded perception and pickup memory, dynamic football roles, rocket/dash/shotgun combat, enemy difficulty setup, and pause display. Completion ends after source freeze, the one authorized production preparation/bake, one authoritative generated build, relevant EditMode tests, generated-output acceptance, and separate-process semantic validation. The user's single five-minute playtest follows this automated handoff; there is no mid-run playtest gate.

## Scope

- in: B1-B6 bots and difficulty; consumer-owned intent APIs; bot-only aim; deterministic pure rules/tests; builder-authored navigation graph; serialized sensing/role composition; generated Player prefab/MovementLab scene; setup and pause screens; final production proof.
- out: A6 shotgun inventory HUD and hit marker; multiplayer/networking, combat-assist scoring, audio/dry-fire sound, right-hand weapon replacement framework, NavMesh/package changes, hidden pickup respawn knowledge, difficulty-based health/damage/speed/cooldown/pickup changes, production UI framework/refactor, new bot markers, broad visual polish, and any earlier completed A1-A5 shotgun redesign.

## Decisions

- assumption: implementation baseline is current `bots` commit `c275c092ff0a042241034a062fe5a4beb389a4a4`; accepted plan blob must be pinned before dispatch. Execution worktree starts exactly there; later user changes require fresh baseline and plan re-acceptance.
- decision: keep `PlayerInputReader` device-only. Add one-fixed-step programmatic requests to `PlayerMotor`, `BallKick`, and `RocketLauncher`; retain the existing `ShotgunWeapon.RequestFire`. Each consumer validates, consumes, clears, applies cooldowns/events, and owns side effects.
- decision: one `BotController` on the shared Player prefab is the non-local aim and intent owner. `[DefaultExecutionOrder(-200)]` lets it publish intent before `BallKick` at `-100` and ordinary fixed-step leaves. It writes root yaw plus clamped `Head.localRotation` in `FixedUpdate`; `PlayerLook` remains the mutually exclusive local-only writer.
- decision: `ParticipantState` remains the per-slot gate owner. All weapon/kick leaves receive the match/alive gate; device input/look receive it only for the local slot; `BotController` receives it only for non-local slots. The new Paused state pauses participant lifecycle timers without using global `Time.timeScale`; existing GoalFreeze and kickoff lifecycle timing remains unchanged.
- decision: difficulty is an enum selected and locked by `MatchController`; allies always receive Medium and all enemies receive the selected Low/Medium/High value. Pure static parameter profiles are code, not ScriptableObjects, so there are no new generated profile assets.
- decision: default profiles are playtest starting points: Low `(reaction 0.45s, decision 0.35s, jitter 0.12s, aim noise 7 degrees, prediction error 0.35)`; Medium `(0.22s, 0.20s, 0.06s, 3 degrees, 0.15)`; High `(0.10s, 0.12s, 0.03s, 1.5 degrees, 0.06)`. Explicit slot/decision/channel hashes provide deterministic samples; no `UnityEngine.Random` or hidden awareness differs by tier.
- decision: navigation is scene-owned serialized directed graph plus deterministic A*. Nodes distinguish Floor, RampDeck, GoalRecess; edges distinguish Walk, Ramp, Drop, ShieldGate. Bots steer only through active corridor or same-area terminal target. No NavMesh, runtime discovery, or ledge shortcut.
- decision: builder creates three-lane floor graph at Z `-28/0/28` and X `-52/-12/0/12/52`, stable spawn/pickup/goal anchors, and ramp anchors from `ramp.position + ramp.rotation * (0,0.25,-8/+8)`. Never call `TransformPoint(0,0.25,-8/+8)` because authored ramp scale would multiply offset. High-to-floor drops remain one-way and clearance-validated. Mouth/recess edges use exact shield collider gate; enabled shield closes route.
- decision: goal shields have no runtime opener and stay enabled. Production graph must report both recesses unreachable; disabled-gate traversal exists only as deterministic rule test/future contract. Automated/manual completion requires ramps, floor lanes, goal-mouth defense, and no shield bypass—not entering recesses.
- decision: perception samples only serialized references. Opponent, ball, and pickup observations use a 75m, 130-degree LOS cone and expire after 1.5s when unseen. Pickup return time is remembered only after a bot witnesses available-to-unavailable transition; coordinated reset clears memory. Unseen `RespawnRemaining` is never read.
- decision: one coordinator per team owns role assignment. Living bots receive unique Attacker/Support/Defender roles where roster size permits; at least one of two-or-more living bots remains Defender. Re-evaluate every 0.5s, hold roles for 2.0s, require a 0.15 normalized score margin to switch, and break ties by slot ID.
- decision: football utility dominates combat. Own-goal emergency/defense and reachable scoring actions outrank support positioning, pickups, and opportunistic enemy damage. Yield shotgun within 12m to the local human who lacks one; yield health within 10m to a teammate at or below 30% health who is more injured.
- decision: combat uses constant-speed intercept plus deterministic difficulty error. Shotgun is eligible against a visible enemy within 16m or the ball within 20m; dash is eligible for a reachable ball/contact target within 2.2m; rocket is the default ranged action when its predicted line is clear and no ally is inside a 1.5m safety corridor. High-only rocket jump is an authored grounded vertical-route heuristic, never a ballistic solver.
- decision: weapon impulses that actually affect the ball call `BallMotor.RecordParticipantTouch(owner)` so rocket/shotgun bot ball control participates in the existing goal/own-goal attribution contract.
- decision: append `Setup=6`, `Paused=7`; preserve ordinals `0..5`. Setup defaults Medium and defers `BeginNewMatch`. Pause only from Playing; resume directly to Playing. No `Time.timeScale`. Pause freezes match clock, participant death/immunity timers, pickup respawn timers, shotgun pump, ball/player motion, and active rocket lifetime/flight; preserves transforms, velocities, scores, phase, cooldown values, and projectiles. Escape cursor captured-to-released edge requests pause; no input-action asset change.
- decision: add `MovementLabBotPipeline.cs` for graph/composition constants instead of changing `MovementLabContractCatalog.cs`; this avoids coupling navigation data to the catalog's lighting fingerprint. Bump `SerializedContractVersion` `5 -> 6`, material-prefab contract `9 -> 10`, and gameplay-scene contract `14 -> 15`; keep manifest schema `8`.
- decision: generated-output acceptance permits builder-traced task-declared semantic changes. Exact changed-set/header coverage, zero unknown types, no dangling-reference increase, stable existing GUIDs, intact asset/`.meta` pairs, separate regeneration commit, workflow provenance, tests, and reload validation remain mandatory. Binary hashes prove provenance only; no byte-equality gate or duplicate build.
- question: None

## Execution Graph

`START -> {T1 -> CP1 -> {T2 -> CP2 || T3 -> CP3 || T5 -> CP5} || T4 -> CP4 -> T6 -> CP6}; CP1 + CP3 + CP5 -> T7 -> CP7; CP1 + CP2 + CP4 -> T8 -> CP8; CP2 + CP3 + CP4 + CP5 + CP6 + CP7 + CP8 -> JOIN1 -> T9 -> CP9 -> T10 -> CP10 -> T11 -> CP11 -> T12 -> CP12 -> FINAL`

- notation: `->` sequential; `||` parallel; `{...}` parallel fan-out/fan-in; `+` requires every named predecessor
- gates: `START` -> accepted baseline in isolated short-path worktree; T1/T4 ownership disjoint. `CP1` -> cross-lane Bot DTOs frozen. `CP4` -> consumer APIs frozen before navigation reads PlayerMotor facts or combat consumes launcher/shotgun seams. `CP5` -> match APIs frozen before perception/coordinator integration. `JOIN1` -> every BotController input producer accepted and source-frozen. `CP10` -> all runtime owners integrated and source-frozen. `CP11` -> all source producers accepted before project mutation. `CP12` -> fresh reviewer accepts frozen generated commit plus attached comparator/transition/test/workflow evidence. `FINAL` -> clean regenerated SHA passes reload validation; zero live child agents; pinned plan artifact unchanged.

## Tasks

### T1: Freeze shared bot contracts

- objective: freeze minimal immutable DTO surface shared by independent bot lanes.
- covered_requirements: B1-B6 shared contract surface; D7-D11 type seams
- owner: `luna_max` implementation worker
- dependencies: `None`; starts at START because contract authoring does not need consumer API changes
- parallel_contract: outputs exact difficulty, observation, aim, target, and combat DTOs; owns only `BotContracts.cs`; disjoint from concurrent T4
- owns: `Assets/_Game/Scripts/Runtime/Bots/BotContracts.cs`
- protected: `Assets/_Game/Scripts/Runtime/Participants/ParticipantReadModel`; existing match/weapon constants; all Unity assets and `.meta` files until T12 import
- read_paths: `Assets/_Game/Scripts/Runtime/Match/MatchRules.cs -> immutable value-type style`; `Assets/_Game/Scripts/Runtime/Weapons/ShotgunDamageRules.cs -> deterministic input/result style`; `Assets/_Game/Scripts/Tests/EditMode/RocketFooxball.EditModeTests.asmdef -> runtime test boundary`
- validation_environment: source-only isolated worktree; no Unity lease or project mutation
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: add explicit ordinals: `BotDifficulty { Low=0, Medium=1, High=2 }`; `BotRole { Attacker=0, Support=1, Defender=2 }`; `BotSampleChannel { ReactionDelay=0, DecisionDelay=1, PredictionError=2, AimConeRadius=3, AimConeAzimuth=4 }`; `BotPickupKind { Health=0, Shotgun=1, Ammo=2 }`; `BotTargetKind { None=0, OwnGoalEmergency=1, DefenderCoverage=2, AttackerBall=3, SupportLane=4, HealthPickup=5, ShotgunPickup=6, AmmoPickup=7, EnemyOpportunity=8, BallFallback=9 }`; `BotCombatAction { None=0, DashKick=1, FireShotgun=2, FireRocket=3, RocketJump=4 }`; `BotCombatTarget { None=0, Ball=1, Enemy=2, SelfImpact=3 }`.
- implementation: immutable constructor/get-property structs: `BotDifficultyParameters(float reactionSeconds,float decisionSeconds,float scheduleJitterSeconds,float aimNoiseDegrees,float predictionErrorFraction)`; `BotBallObservation(bool hasObservation,bool isVisible,bool isGrounded,Vector3 position,Vector3 velocity,float ageSeconds)`; `BotParticipantObservation(bool hasObservation,bool isVisible,int slotId,ParticipantTeam team,bool isLocalParticipant,bool isAlive,Vector3 position,Vector3 velocity,float health,float maxHealth,bool hasShotgun,int shotgunShells,int shotgunShellCapacity,float ageSeconds)`; `BotPickupObservation(int stableId,BotPickupKind kind,Vector3 position,bool hasObservation,bool isVisible,bool isAvailable,bool hasWitnessedRespawn,float estimatedRespawnRemaining,float ageSeconds)`; `BotAimSolution(bool isValid,bool usedIntercept,Vector3 aimPoint,Vector3 direction,float interceptTime)` plus finite-zero `Invalid`; `BotTargetKey(BotTargetKind kind,int subjectId)`.
- implementation: exact `BotCombatInput(BotDifficulty difficulty,BotRole role,BotTargetKind activeTargetKind,float activeTargetScore,bool suppressParticipantCombat,bool preferBallActions,Vector3 actionOrigin,Vector3 ballRocketLaunchPosition,Vector3 enemyRocketLaunchPosition,Vector3 rocketJumpLaunchPosition,Vector3 routeForwardXZ,BotBallObservation ball,BotParticipantObservation enemy,BotParticipantObservation allyA,BotParticipantObservation allyB,bool ballRouteReachable,bool enemyRouteReachable,bool dashReady,bool shotgunReady,bool launcherReady,BotAimSolution ballDirectAim,BotAimSolution ballRocketAim,BotAimSolution enemyDirectAim,BotAimSolution enemyRocketAim,bool rocketLineClearToBall,bool rocketLineClearToEnemy,bool isGrounded,bool upwardTransitionRequired,Vector3 rocketJumpAimPoint,bool rocketJumpLineClear)` with matching getters. `actionOrigin` is current Head position used for range/direct aim; each launch position is the exact direction-dependent cached T4 pose for its named candidate; `routeForwardXZ` is normalized current navigator route direction or zero. `BotCombatResult(BotCombatAction action,BotCombatTarget target,Vector3 aimPoint,Vector3 aimDirection,Vector3 launchPosition)` exposes `HasAction` and finite-zero `None`; non-rocket actions return zero launch position. No arrays, collections, `MonoBehaviour`, collider, owner reference, scene lookup, navigation record, or control-intent type.
- done when: every cross-lane value compiles from one immutable file; difficulty cannot encode health/damage/speed/cooldown/pickup changes; subsystem-local navigation/role-rule state remains outside T1.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Bots/BotContracts.cs -> clean patch syntax/whitespace for shared contract file; authoritative Unity compile remains T12`
- review_focus: missing fields or mutable/Unity-query behavior would force fan-out workers to redefine contracts or share ownership; inspect type completeness, immutability, and fairness boundaries.
- review_checkpoint: CP1

### T2: Implement deterministic difficulty and aim rules

- objective: own tier parameters, stable schedules, direct aim, constant-speed intercept, prediction error, and aim noise as pure deterministic rules.
- covered_requirements: B1 difficulty/aim rules; D8-D9
- owner: `luna_max` implementation worker
- dependencies: `CP1 -> deterministic rules require the reviewed BotContracts field/signature surface`
- parallel_contract: outputs `BotDifficultyRules` and `BotAimRules` consumed by T8/T9; disjoint from concurrent T3/T5
- owns: `Assets/_Game/Scripts/Runtime/Bots/BotDifficultyRules.cs`; `Assets/_Game/Scripts/Runtime/Bots/BotAimRules.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotDifficultyRulesTests.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotAimRulesTests.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotInterceptRulesTests.cs`
- protected: T1 `BotContracts.cs`; existing match/weapon constants; every concurrent T3/T5 owned path
- read_paths: `Assets/_Game/Scripts/Runtime/Bots/BotContracts.cs -> frozen inputs/results`; `Assets/_Game/Scripts/Runtime/Match/MatchRules.cs -> pure-logic style`; `Assets/_Game/Scripts/Runtime/Weapons/ShotgunDamageRules.cs -> finite deterministic rule style`
- validation_environment: source-only isolated worktree; no Unity lease or project mutation
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: `BotDifficultyRules.GetParameters` returns exact Decisions tuples; unknown enum -> Medium. `StableHash(slotId,ordinal,channel)` uses unchecked FNV-1a: seed `2166136261u`; for slot, ordinal, channel: `(hash ^ (uint)value) * 16777619u`. Pin `(0,0,0)=1253111735`, `(1,0,0)=197805164`, `(1,2,3)=1456420779`, `(-1,7,4)=3160143677`. `Sample01=(hash & 0x00ffffff)/16777216f`; signed sample `2*Sample01-1`.
- implementation: reaction/decision delay = `max(0.01, base + signedSample*max(0,jitter))`; invalid base -> `0.01`; invalid jitter -> zero. No third aim timer. T9 reaction phase copies sensor state; decision phase uses latest reacted copy and refreshes aim once.
- implementation: `BotAimRules.TrySolveIntercept(origin,targetPosition,targetVelocity,projectileSpeed,out time,out point)` uses double-precision quadratic `a=v·v-s²`, `b=2r·v`, `c=r·r`; `c<=1e-6` -> time zero; `|a|<=1e-6` -> positive `-c/b`; otherwise smallest nonnegative finite root; invalid/no-root -> false and finite-zero outputs. `ApplyPredictionError(idealTime,maxFraction,slot,ordinal)` returns `max(0,idealTime*(1+signed PredictionError*clamp01(maxFraction)))`; invalid time -> zero. Recompute point as `targetPosition+targetVelocity*perturbedTime`.
- implementation: `ApplyAimNoise(direction,maxDegrees,slot,ordinal)` uses normalized input; reference axis `Vector3.up` when `abs(dot(direction,up))<0.999`, else `Vector3.right`; right=`normalize(cross(reference,direction))`; up=`cross(direction,right)`; cone angle=`sqrt(Sample01(AimConeRadius))*clamp(maxDegrees,0,89)`, azimuth=`2π*Sample01(AimConeAzimuth)`. `ClampPitch` defaults `[-80,80]`. `SolveDirectAim` maps dash/shotgun ball/enemy targets to direct noisy aim. `SolveInterceptAim` maps rocket ball/enemy targets through ideal intercept -> perturbed time/point -> cone -> pitch. After noise/pitch, set `AimPoint=origin+finalDirection*max(.01,distance(origin,predictedPoint))`; point/direction stay collinear. Invalid math -> `BotAimSolution.Invalid`, never fireable fallback; error applied once.
- done when: exact tuples/hashes/schedules are pinned; intercept stationary/lateral/receding/coincident/linear/no-root/invalid cases pass; successful directions finite normalized; difficulty affects timing/error only.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Bots/BotDifficultyRules.cs Assets/_Game/Scripts/Runtime/Bots/BotAimRules.cs Assets/_Game/Scripts/Tests/EditMode/BotDifficultyRulesTests.cs Assets/_Game/Scripts/Tests/EditMode/BotAimRulesTests.cs Assets/_Game/Scripts/Tests/EditMode/BotInterceptRulesTests.cs -> clean patch syntax/whitespace for difficulty/aim/intercept rules and tests; authoritative Unity compile/tests remain T12`
- review_focus: unstable hash, duplicate error application, nonfinite fallback, or intercept direct-fire fallback breaks deterministic tier behavior; inspect exact formulas, channels, roots, normalization, and tests.
- review_checkpoint: CP2

### T3: Implement deterministic role and target rules

- objective: own unique team-role assignment, hysteresis, football-first target ordering, and pickup etiquette as pure rules.
- covered_requirements: B1/B4 role and target logic; D7-D9
- owner: `luna_max` implementation worker
- dependencies: `CP1 -> rule DTOs require frozen Bot enums/observations/target keys`
- parallel_contract: outputs `BotRoleRules` and `BotTargetRules` consumed by T7/T9; disjoint from T2/T5
- owns: `Assets/_Game/Scripts/Runtime/Bots/BotRoleRules.cs`; `Assets/_Game/Scripts/Runtime/Bots/BotTargetRules.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotRoleRulesTests.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotTargetRulesTests.cs`
- protected: T1/T2 files; concurrent T5 files; runtime owners
- read_paths: `Assets/_Game/Scripts/Runtime/Bots/BotContracts.cs -> frozen DTOs`; `Assets/_Game/Scripts/Runtime/Match/MatchRules.cs -> pure-rule style`; `plans/3v3-bots-combat-design-handoff.md -> football-first and etiquette rules`
- validation_environment: source-only isolated worktree; no Unity lease or mutation
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: exact pure types/APIs: `BotRoleContext(bool hasBallObservation,Vector3 ballPosition,Vector3 ownGoalPosition,Vector3 enemyGoalPosition,bool aliveSetChanged)`; `BotRoleCandidate(int slotId,bool isLocalParticipant,bool isAlive,Vector3 position,bool hasCurrentRole,BotRole currentRole,float heldSeconds)`; `BotRoleAssignment(bool isAssigned,int slotId,BotRole role,float score)`; `BotRoleAssignmentSet(first,second,third)` sorted by slot, derived Count, `TryGetAssignment(int,out BotRoleAssignment)`. `BotRoleRules.Assign(context,first,second,third)` filters `!isLocalParticipant && isAlive`. Effective ball = observed finite ball when present, otherwise midfield `(ownGoalPosition+enemyGoalPosition)*.5`; invalid goal input returns empty set. Fitness=`1-clamp01(distance/75)` from candidate to effective ball for Attacker, own goal for Defender, and `(effectiveBall+ownGoalPosition)*.5` for Support. Legal sets: one bot Defender; two Attacker+Defender; three Attacker+Support+Defender. Max mean; equal totals compare role ordinals lexicographically for ascending slots. Coordinator computes alive bitmask and sets `aliveSetChanged` against prior evaluation; true bypasses hysteresis. Otherwise retain legal map while any held `<2s`; switch only at best >= current +`0.15`.
- implementation: exact `BotTargetCandidate(BotTargetKey key,float score,Vector3 navigationPosition,Vector3 actionPosition,bool suppressParticipantCombat,bool preferBallActions,bool reachable,float routeCost,float expiresAt)` and `BotTargetSelection(bool hasTarget,BotTargetKey key,float score,Vector3 navigationPosition,Vector3 actionPosition,bool suppressParticipantCombat,bool preferBallActions,float routeCost,float expiresAt)` plus finite-zero None. `BotTargetRules.Consider(current,candidate)` rejects invalid/unreachable/nonfinite/negative-cost or nonfinite/negative-expiry candidates; order score descending, route cost ascending, kind ordinal, subject ID. Score contract: emergency `100`, defender `90`, attacker `70`, support `55`, health `35`, shotgun/ammo `25`, visible enemy `20`, recent enemy `15`, fallback ball `0`. Flags: emergency/attacker/fallback `true,true`; defender `true,true` only with valid ball in own half else `false,false`; support/pickup/enemy `false,false`. Human shotgun yield requires same-team living local human without shotgun within `12m`. Health yield requires same-team living teammate within `10m`, ratio `<=0.30`, strictly lower ratio; equal ratio yields to lower slot.
- done when: 0/1/2/3 living sets, defender reservation, death reassignment, hold/margin boundaries, score ladder, invalid candidates, route/ID ties, and inclusive etiquette boundaries pass.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Bots/BotRoleRules.cs Assets/_Game/Scripts/Runtime/Bots/BotTargetRules.cs Assets/_Game/Scripts/Tests/EditMode/BotRoleRulesTests.cs Assets/_Game/Scripts/Tests/EditMode/BotTargetRulesTests.cs -> clean patch syntax/whitespace for role/target rules and tests; authoritative Unity compile/tests remain T12`
- review_focus: illegal duplicate roles, unstable ties, hysteresis on changed roster, or utility inversion can remove defense or promote combat over ball play; inspect legal sets, thresholds, candidate rejection, and total ordering.
- review_checkpoint: CP3

### T4: Add consumer-owned programmatic request APIs

- objective: let a later bot integration owner request movement, jump, kick, and rocket fire for one fixed step while each existing gameplay consumer keeps validation, cooldowns, events, and side effects.
- covered_requirements: B2 control seam; D1-D6
- owner: `luna_max` implementation worker
- dependencies: `None`; starts at START because existing consumer components and the Shotgun request precedent fully define this seam without Bot contracts
- parallel_contract: outputs exact one-step movement/jump/kick/rocket/shotgun APIs plus launcher pose/speed/readiness for T8/T9; disjoint from T1
- owns: `Assets/_Game/Scripts/Runtime/Movement/PlayerMotor.cs`; `Assets/_Game/Scripts/Runtime/Ball/BallKick.cs`; `Assets/_Game/Scripts/Runtime/Weapons/RocketLauncher.cs`; `Assets/_Game/Scripts/Runtime/Weapons/ShotgunWeapon.cs`
- protected: `PlayerInputReader`; local input semantics; `DashKickRules`; weapon cooldown/damage/immunity/event ownership; T1 contracts; every concurrent T2/T3/T5 owned path
- read_paths: `Assets/_Game/Scripts/Runtime/Weapons/ShotgunWeapon.cs/RequestFire -> existing consumer request pattern`; `Assets/_Game/Scripts/Runtime/Movement/PlayerLook.cs -> local aim ownership`; `Assets/_Game/Scripts/Runtime/Participants/ParticipantState.cs -> later gate integration`
- validation_environment: source-only isolated worktree; no builder or generated mutation
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: `PlayerMotor`: add `bool SetMoveIntent(Vector2)` and `bool RequestJump()`. Accept only enabled active component and finite values; move clamps magnitude 1 and latest valid wins, including zero. At enabled `FixedUpdate`, snapshot+clear programmatic fields before dash branch; active dash discards them. Otherwise programmatic move overrides device move for one step; programmatic jump ORs consumed device jump into existing buffer/coyote path. Invalid input does not replace pending valid input. `OnDisable`, disabled/null-controller exit, `SetSimulationEnabled(false)`, `ClearQueuedState`, and reset clear payload/flags.
- implementation: `BallKick`: add `bool RequestKick(Vector3 aim)`. Accept enabled/simulation-enabled finite nonzero aim; normalize; latest valid wins. FixedUpdate ticks cooldown, consumes local edge, snapshots+clears request, gives programmatic aim precedence, then uses existing `CanActivate -> TryStartDash -> cooldown/contact clear/DashStarted`. Blocked attempt drops. Active-dash request may steer current dash but never restarts/emits second event. Disable/gate/reset clear request.
- implementation: `RocketLauncher`: add `bool RequestFire(Vector3 launchPosition,Vector3 direction)`, `float ProjectileSpeed`, `bool TryGetLaunchPosition(Vector3 fallbackOrigin,Vector3 direction,out Vector3 launchPosition)`. `ProjectileSpeed` returns serialized projectile prefab `Speed` only when prefab exists and speed is finite positive; otherwise zero. Pose helper validates enabled/simulation-enabled launcher, finite origin/nonzero direction; base is serialized spawn point when present, otherwise fallback; add normalized direction times nonnegative spawn offset. Programmatic request stores exact already-offset launch position so later root motion cannot change probed pose. Latest valid one-step request wins over local held fire; local wrapper computes pose once; both pass one private core using supplied pose verbatim. Core alone checks `CanFire`, instantiates, initializes, registers, assigns cooldown, cancels immunity, raises event. Disabled/blocked request returns false and drops. Disable/gate/reset clear request; reset destroys tracked rockets.
- implementation: `ShotgunWeapon.RequestFire(origin,direction)` accepts only `isActiveAndEnabled && simulationEnabled && finite origin && finite nonzero direction`; invalid/disabled/gated call returns false without replacing a pending valid request. Latest accepted request wins and is consumed once by existing FixedUpdate core. `OnDisable`, `SetSimulationEnabled(false)`, and `ResetState` clear pending flag plus stored origin/direction. No firing, shell, pump, damage, event, or local-input logic moves out of ShotgunWeapon.
- done when: programmatic requests are bounded to one fixed step, invalid/disabled/reset requests clear, and kick/rocket/shotgun consumers retain all cooldown, event, immunity, and reset ownership without a bot source dependency.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Movement/PlayerMotor.cs Assets/_Game/Scripts/Runtime/Ball/BallKick.cs Assets/_Game/Scripts/Runtime/Weapons/RocketLauncher.cs Assets/_Game/Scripts/Runtime/Weapons/ShotgunWeapon.cs -> clean patch syntax/whitespace for all consumer request APIs; authoritative Unity compile remains T12`
- review_focus: bypassing consumer gates, stale requests, or two launch paths can duplicate attacks, skip immunity/cooldown, or break local input; inspect consume/clear order and the single execution core per consumer.
- review_checkpoint: CP4

### T5: Add difficulty setup and match-owned pause UI core

- objective: require enemy difficulty selection before kickoff, lock it for the match, keep allies Medium, and show the locked choice on match-owned Setup and Paused screens without changing participant integration yet.
- covered_requirements: B6 difficulty/setup/pause UI; difficulty lock and display
- owner: `luna_max` implementation worker
- dependencies: `CP1 -> setup state requires frozen BotDifficulty enum`
- parallel_contract: outputs Setup/Paused transitions, locked difficulty, `GetBotDifficulty(team)`, and HUD flow consumed by T9/T10; disjoint from T2/T3
- owns: `Assets/_Game/Scripts/Runtime/Match/MatchRules.cs`; `Assets/_Game/Scripts/Runtime/Match/MatchController.cs`; `Assets/_Game/Scripts/Runtime/Hud/MatchHud.cs`; `Assets/_Game/Scripts/Tests/EditMode/MatchRulesTests.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotMatchSetupRulesTests.cs`
- protected: existing ordinals 0-5, score/reset ownership, shotgun HUD behavior, T1 types, `ParticipantState`, `BotController`, pickup/shotgun/projectile pause adapters deferred to T10, Input System asset, concurrent T2/T3 files
- read_paths: `Assets/_Game/Scripts/Runtime/Input/PlayerInputReader.cs/CursorCaptured -> Escape pause signal`; `Assets/_Game/Scripts/Runtime/Hud/MatchHud.cs/UpdateFinalCursorOverride -> cursor precedent`; `Assets/_Game/Scripts/Runtime/Match/MatchController.cs/BeginNewMatch,ApplyGameplayGate -> lifecycle owner`
- validation_environment: source-only isolated worktree; lifecycle fan-in deferred to T10; serialized wiring deferred to T11
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: append match `Setup=6`, `Paused=7`; append HUD `Setup=9`, `Paused=10` after existing `Final=8`. Add pure `IsSupportedDifficulty`, select/start/pause/resume/rematch validity, `ShouldAdvancePausedTimer(MatchState state) => state != Paused`, and `ResolveBotDifficulty(participantTeam,localTeam,lockedEnemyDifficulty)`. Controller properties: `SelectedEnemyDifficulty`, `LockedEnemyDifficulty`, `DifficultyLocked`; public `event Action<bool> PauseChanged`. Public APIs: `bool TrySelectEnemyDifficulty(BotDifficulty)`, `bool TryStartConfiguredMatch()`, `bool TryPauseMatch()`, `bool TryResumeMatch()`, `BotDifficulty GetBotDifficulty(ParticipantTeam)`. Invalid call -> false/no event or mutation. Unlocked/invalid opponent query -> Medium. `Start` enters Medium/unlocked Setup, gameplay off, no reset. Start locks then `BeginNewMatch(MatchStart)`. Rematch Final -> fresh Medium/unlocked Setup. MatchRules tests pin paused false and every other state true for timer advancement.
- implementation: pause only Playing with positive clock; T5 sets Paused then raises `PauseChanged(true)` without calling destructive `ApplyGameplayGate`, reset, destroy, score, or phase methods. Resume enters Playing then raises `PauseChanged(false)`; T10 inserts exact pause-only leaf adapters around those transitions before any Unity run. Legacy void `ResetMatch()` and `ResetMatchAndScore()` return unchanged in Setup, Paused, or Final; their existing behavior remains only in states `0..5`, so neither bypasses setup/pause/rematch. Tests invoke both legacy methods in Setup/Paused/Final and assert state, score, difficulty lock, and events unchanged. Setup/Paused screen precedence beats death/table/GO. HUD keeps exactly match/local/input serialized refs; snapshots selected/locked/lock values; setup buttons Low/Medium/High/Start; pause shows locked tier and Resume. Cursor override applies Setup/Paused/Final. Pause request only on captured-to-released edge during Playing; restore captured hidden cursor on Start/Resume; OnDisable restores saved state and clears edge initialization.
- done when: no kickoff occurs before Start; selection cannot change after lock; the match exposes one team-derived difficulty assignment call; pause preserves clock/motion without global time scale and drives the separate participant pause-lifecycle seam; GoalFreeze/kickoff never drive that seam; the locked tier displays; resume restores Playing; rematch returns to Setup; ordinal and pure transition tests pass.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Match Assets/_Game/Scripts/Runtime/Hud/MatchHud.cs Assets/_Game/Scripts/Tests/EditMode/MatchRulesTests.cs Assets/_Game/Scripts/Tests/EditMode/BotMatchSetupRulesTests.cs -> clean patch syntax/whitespace for setup/pause core and tests; authoritative Unity compile/tests remain T12`
- review_focus: starting before setup, mutable in-match difficulty, or a second gameplay gate owner can desynchronize match clock and motion; inspect transitions, gate calls, cursor flow, team derivation, and preserved shotgun HUD behavior.
- review_checkpoint: CP5

### T6: Implement multi-level graph navigation core

- objective: own deterministic routing and safe corridor steering across floor lanes, ramps, one-way drops, and shield-gated recesses without coupling to BotController.
- covered_requirements: B3 navigation; D11
- owner: `luna_max` implementation worker
- dependencies: `CP4 -> navigation binds the accepted PlayerMotor/CharacterController movement seam instead of reading a concurrently changing consumer`
- parallel_contract: outputs graph records plus probe/steering APIs consumed by T9/T11; runs after T4 and remains path-disjoint from T1-T5
- owns: `Assets/_Game/Scripts/Runtime/Bots/BotNavigationGraph.cs`; `Assets/_Game/Scripts/Runtime/Bots/BotNavigationRules.cs`; `Assets/_Game/Scripts/Runtime/Bots/BotNavigator.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotNavigationRulesTests.cs`
- protected: T1-T5 files; arena collision geometry; builder sources until T11
- read_paths: `Assets/_Game/Editor/MovementLab/MovementLabArenaPipeline.cs/BuildArena,BuildGoal -> authoritative geometry`; `Assets/_Game/Scripts/Runtime/Movement/PlayerMotor.cs -> CharacterController motion facts`; `Packages/manifest.json -> no navigation package`
- validation_environment: source-only isolated worktree; graph wiring deferred to T11/T12
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: `BotNavigationGraph : MonoBehaviour` hosts serialized graph. Explicit `BotNavigationArea { Floor=0,RampDeck=1,GoalRecess=2 }`, `BotNavigationTraversal { Walk=0,Ramp=1,Drop=2,ShieldGate=3 }`, `[Serializable] BotNavigationNodeRecord(int id,Vector3 position,BotNavigationArea area,float safeRadius)`, `[Serializable] BotNavigationEdgeRecord(int id,int fromNodeId,int toNodeId,BotNavigationTraversal traversal,float cost,float corridorHalfWidth,Collider gateCollider)`. Graph serializes exact Player controller facts: radius `.4`, height `1.8`, center `(0,.9,0)`, slope `60`, step `.3`, skin `.04`; exposes count/index getters only. `BotNavigator` serializes graph, Default-only ground mask `1`, arrival `.75`, vertical arrival `.65`, lookahead `3`, target replan `1.5`, corridor margin `1`, ledge probe `1.5`, ledge depth `2.5`.
- implementation: records use Unity-serializable private `[SerializeField]` fields plus public get-only properties and exact constructors; graph fields are `BotNavigationNodeRecord[] nodes`, `BotNavigationEdgeRecord[] edges`, controller facts, and `bool TryValidate(out string reason)`. Validation: Walk joins same area, except Floor<->RampDeck only when vertical delta <= step+skin; Ramp joins RampDeck<->RampDeck; Drop joins RampDeck->Floor with descending Y and no reverse; ShieldGate joins Floor<->GoalRecess with nonnull collider. Also require unique nonnegative IDs, finite positions/clearance, valid endpoints, positive cost >= endpoint distance, corridor >= radius+skin, Ramp slope, no high-deck shortcut. `BotNavigationRules` owns `BotPathStatus { Invalid=0,Unreachable=1,Success=2,BufferTooSmall=3 }`; `BotNavigationStatus { Invalid=0,Unreachable=1,Following=2,Arrived=3 }`; `BotVerticalRoute { None=0,Ascend=1,Descend=2 }`; immutable `BotNavigationSteeringResult(status,steeringPoint,worldDirection,verticalRoute)` with HasSteering/Arrived. Invalid graph logs `BotNavigator requires a valid serialized BotNavigationGraph: <reason>.` and disables navigator; graph remains data host.
- implementation: exact pure path API: `BotPathStatus BotNavigationRules.FindPath(BotNavigationNodeRecord[] nodes,BotNavigationEdgeRecord[] edges,int startNodeId,int goalNodeId,bool[] edgeEnabled,int[] nodePathBuffer,float[] gScoreBuffer,float[] fScoreBuffer,int[] parentNodeBuffer,int[] parentEdgeBuffer,bool[] openBuffer,bool[] closedBuffer,out int nodeCount,out float routeCost)`. `edgeEnabled.Length > max edge ID`; every node scratch buffer length >= node count; caller owns all arrays. Null/mismatched buffers -> Invalid; valid route longer than `nodePathBuffer` -> BufferTooSmall with count0/cost0. `BotNavigator` allocates/resizes these arrays only after graph validation when node/edge counts change, never during probe/evaluation. Allocation-bounded A* uses linear open scan, Euclidean 3D heuristic, directed edges, gate state copied into reusable `edgeEnabled`. Ties within `1e-5`: lower f, node ID, parent node ID, edge ID. Nearest node uses 3D distance then node ID. Public runtime APIs: `bool TryProbeTarget(Vector3 worldTarget,out Vector3 projectedTarget,out float routeCost)`; `BotNavigationSteeringResult EvaluateSteering(Vector3 worldTarget)`; `void ClearRoute()`.
- implementation: corridor XZ projection clamps lateral offset inside `edgeHalfWidth-radius-skin`; look-ahead `3m`; endpoint convergence; max one edge advancement per call. Replan on target node change, target move >=`1.5m`, start-node change with no active edge, remaining gate change, unreachable gate-hash change, or corridor escape. Edge arrival uses horizontal `min(0.75,to.safeRadius)` plus vertical `0.65`; terminal arrival horizontal `0.75`. After goal node, direct terminal steering only when target nearest node ID+area still match: Floor/GoalRecess -> exact XZ; RampDeck -> incident Ramp corridor only. Never use target object Y for terminal arrival.
- implementation: before non-Drop steering, spherecast radius `.36` from `transform.position + desiredXZ*1.5 + up*(stepOffset+radius+skinWidth)` down `2.5`, Default-only mask `1`, triggers ignored. Accept only `attachedRigidbody==null` and normal within serialized `60°` slope; no hit/steep hit -> zero steering and clear/replan. Drop edges skip forward-ground requirement. This is narrow ledge safety, not graph discovery. Tests cover malformed graph, deterministic scrambled-order ties, floor/ramp paths, drops, gates, unreachable/buffer status, corridor formulas, one-edge advancement, terminal same-area rule, vertical route, nonfinite inputs; builder validator owns serialized mask/tuning, playtest owns physical feel. No scene search, transform mutation, or NavMesh.
- done when: synthetic tests prove deterministic floor/ramp paths, bidirectional ramps, one-way drops, gate state, unreachable handling, terminal same-area steering, and no RampDeck-to-floor shortcut; T9 needs no graph internals.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Bots/BotNavigationGraph.cs Assets/_Game/Scripts/Runtime/Bots/BotNavigationRules.cs Assets/_Game/Scripts/Runtime/Bots/BotNavigator.cs Assets/_Game/Scripts/Tests/EditMode/BotNavigationRulesTests.cs -> clean patch syntax/whitespace for navigation core/tests; authoritative Unity compile/tests remain T12`
- review_focus: flat-height assumptions, reverse drop edges, direct target steering, or leaking path state to BotController can strand/fall bots and invalidate shield collision; inspect transitions, gate checks, corridor clamp, ties, and fallback API.
- review_checkpoint: CP6

### T7: Add bounded perception, pickup memory, and team-owned roles

- objective: own visible/recent knowledge, witnessed pickup timing, deterministic team roles, ally safety copies, and pickup etiquette behind narrow read APIs for T9.
- covered_requirements: B4 perception and roles; D7; football-first priorities and pickup knowledge/etiquette
- owner: `luna_max` implementation worker
- dependencies: `CP1 + CP3 + CP5 -> sensors publish frozen snapshots; coordinator consumes accepted role/target rules and the frozen MatchController setup/pause/difficulty API`
- parallel_contract: outputs copy-only observations, pickup candidates, ally safety snapshots, and sole team role map consumed by T9/T11
- owns: `Assets/_Game/Scripts/Runtime/Bots/BotPerception.cs`; `Assets/_Game/Scripts/Runtime/Bots/BotPerceptionMemory.cs`; `Assets/_Game/Scripts/Runtime/Bots/BotTeamRoleCoordinator.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotPerceptionMemoryTests.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotRoleCoordinatorRulesTests.cs`
- protected: T1/T3 contracts/rules; T6 graph ownership; unseen `ArenaPickup.RespawnRemaining`; match state; `BotController`; `ParticipantState`
- read_paths: `Assets/_Game/Scripts/Runtime/Pickups/ArenaPickup.cs -> visible availability/reset contract`; `Assets/_Game/Scripts/Runtime/Participants/ParticipantSpawnSet.cs -> LOS-mask precedent`; `Assets/_Game/Scripts/Runtime/Match/MatchController.cs -> serialized owner reads`; `Assets/_Game/Scripts/Runtime/Bots/BotRoleRules.cs -> accepted role scoring API`
- validation_environment: source-only isolated worktree; scene arrays and masks deferred to T11
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: `BotPerception` `[DefaultExecutionOrder(-300)]`; serialized self, match, ball, Head, six-slot roster, ordered pickups `0 health west,1 health east,2 shotgun center,3 ammo west,4 ammo east`, own/enemy goals, obstacle mask, `75m/130°/1.5s`. Full FOV `130°` -> accept `dot(forward,toTarget)>=cos(65°)`. Validate counts/uniqueness/teams/exact pickup kinds/order/goals/tuning; disable with exact error. FixedUpdate advances private gameplay clock only while Playing. LOS uses reusable `RaycastHit[8]` + `Physics.RaycastNonAlloc`, exact distance, triggers ignored, mask excluding Participants/Projectiles. Ignore colliders belonging to intended target root/rigidbody; nearest other hit before `distance-.05` blocks; no other hit succeeds. Sample ball/opponents only after LOS; ball copy includes grounded; participant copies include inventory/capacity. Visible dead opponent invalidates. Recent expires age >`1.5s`. Same-team safety/etiquette copies health, local identity, inventory, capacity, position/velocity regardless LOS but never becomes opponent target. No runtime search.
- implementation: exact per-observer public surface: `int ObserverSlotId`, `float GameplayTime`, `Vector3 OwnGoalPosition`, `Vector3 EnemyGoalPosition`, `bool TryGetBallObservation(out BotBallObservation)`, `bool TryGetParticipantObservation(int slotId,out BotParticipantObservation)`, `bool TryGetPickupObservation(int stablePickupId,out BotPickupObservation)`. Participant lookup covers self, same-team allies, and opponents from this observer's copy; invalid index/state -> false/default. Controller reads only its own perception. Coordinator never merges knowledge across observers.
- implementation: `BotPickupMemoryState(bool hasPriorVisibleSample,bool priorVisibleAvailable,bool hasWitnessedRespawn,float remaining)` plus pure `BotPerceptionMemory.UpdatePickup(prior,isVisible,isAvailable,respawnDelay,gameplayDelta)` and `PublishPickup(stableId,kind,position,state,isVisible,isAvailable,age)` own transitions. Consecutive visible Available->Unavailable starts estimate; gameplay delta decrements only while Playing. Before due: HasObservation true, IsVisible false, IsAvailable false, witnessed true, remaining >0. At unseen due: HasObservation true, IsVisible false, IsAvailable true as remembered belief, witnessed true, remaining0. Visible available clears estimate; visible unavailable after due clears witnessed belief and reports unavailable. Unseen gap/first-seen unavailable/invalid delay creates none. Reaction latency copies current remaining/age; never read `RespawnRemaining`. Coordinated reset/disable returns default state.
- implementation: `BotTeamRoleCoordinator` `[DefaultExecutionOrder(-250)]`; serialized team, match, exact three participants, exact nonlocal perceptions, `0.5/2.0/0.15/12/10/0.30`. Local slot never role candidate: Blue assigns slots `1/2`; Red `3/4/5`. Immediate first evaluation; 0.5 gameplay-second cadence; bot alive bitmask change bypasses cadence/hold. Public: `bool TryGetAssignedRole(int botSlotId,out BotRole)`, `int GetPickupCandidateCount(int observerSlotId)`, `bool TryGetPickupCandidate(int observerSlotId,int candidateIndex,out BotTargetCandidate)`. Candidate key uses kind-local subject IDs: health west/east `0/1`, shotgun `0`, ammo west/east `0/1`; mapping from global perception IDs is fixed above. Candidate generation uses observer perception plus same-team etiquette. Add pure `BotCoordinatorScheduleRules.ShouldEvaluate(elapsed,interval,first,aliveSetChanged)` in `BotPerceptionMemory.cs`; tests target helper, not MonoBehaviour.
- done when: bots cannot query unseen pickup countdowns or runtime-search objects; tests cover visibility expiry, witnessed/unwitnessed pickup transitions, reset, role cadence/death publication, and etiquette; T9 consumes only copy APIs.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Bots/BotPerception.cs Assets/_Game/Scripts/Runtime/Bots/BotPerceptionMemory.cs Assets/_Game/Scripts/Runtime/Bots/BotTeamRoleCoordinator.cs Assets/_Game/Scripts/Tests/EditMode/BotPerceptionMemoryTests.cs Assets/_Game/Scripts/Tests/EditMode/BotRoleCoordinatorRulesTests.cs -> clean patch syntax/whitespace for perception/role core/tests; authoritative Unity compile/tests remain T12`
- review_focus: live unseen state reads, runtime discovery, or per-bot self-assignment creates cheating and role conflicts; inspect every data source, expiry/reset, coordinator ownership, deterministic assignment, and published API.
- review_checkpoint: CP7

### T8: Implement football-first combat core and ball attribution

- objective: own pure rocket/dash/shotgun arbitration, friendly safety, High-only rocket-jump eligibility, and weapon-to-ball attribution while leaving orchestration to T9.
- covered_requirements: B5 combat; D10; enemy-rocket knockback exercise; bot weapon/ball goal attribution
- owner: `luna_max` implementation worker
- dependencies: `CP1 + CP2 + CP4 -> arbitration consumes frozen combat DTOs, accepted aim semantics, and frozen launcher/shotgun consumer seams`
- parallel_contract: outputs pure `BotCombatRules` plus accepted-impulse ball attribution consumed by T9/T10; disjoint from T7
- owns: `Assets/_Game/Scripts/Runtime/Bots/BotCombatRules.cs`; `Assets/_Game/Scripts/Runtime/Ball/BallMotor.cs`; `Assets/_Game/Scripts/Runtime/Weapons/ShotgunWeapon.cs`; `Assets/_Game/Scripts/Runtime/Weapons/ExplosionResolver.cs`; `Assets/_Game/Scripts/Tests/EditMode/BotCombatRulesTests.cs`
- protected: T1/T2 contracts; T4 launcher/shotgun seams; damage/knockback/friendly-fire/immunity/cooldown/falloff/spread/dash/movement tuning; `BotController`; `ParticipantState`
- read_paths: `Assets/_Game/Scripts/Runtime/Weapons/RocketLauncher.cs/RequestFire,ProjectileSpeed -> accepted consumer seam`; `Assets/_Game/Scripts/Runtime/Weapons/RocketProjectile.cs/Speed -> intercept input source`; `Assets/_Game/Scripts/Runtime/Weapons/ParticipantRelationship.cs -> target policy`; `Assets/_Game/Scripts/Runtime/Ball/BallMotor.cs/RecordParticipantTouch -> attribution owner`
- validation_environment: source-only isolated worktree; final feel is user playtest, not an automated physics assertion
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: `BotCombatRules.Evaluate(in BotCombatInput)` explicit order. `PreferBallActions`: visible reachable ball Dash `<=2.2m` -> Shotgun `<=20m` -> Rocket with valid intercept/line/corridor -> None; no enemy fallback. Else `SuppressParticipantCombat` -> None. Else High rocket jump when grounded, Ascend route, launcher ready, clear authored self-impact probe/corridor, and no eligible ball action. Then visible enemy Dash `<=2.2m` -> Shotgun `<=16m` -> Rocket; enemy score must be `<=20`. Failed action advances only within same ordered list. Current visibility required to fire; recent enemy is navigation-only. Inclusive boundaries pass.
- implementation: public pure `BotCombatRules.GetRocketJumpDirection(routeForwardXZ)` returns normalized `Vector3.down-normalize(routeForwardXZ)*0.25`, or zero for invalid/zero route. Each rocket candidate uses only its matching input launch pose: ball corridor `BallRocketLaunchPosition -> ballRocketAim.AimPoint`; enemy corridor `EnemyRocketLaunchPosition -> enemyRocketAim.AimPoint`; jump probe/corridor starts at `RocketJumpLaunchPosition`. Radius `1.5m`; endpoints included; degenerate segment -> sphere. Check both living teammates regardless LOS; malformed known-live ally is unsafe. Zero jump direction makes jump ineligible. Probe must hit environment within resolver blast radius and satisfy existing `BlastMath.TryGetUnderfootFacing`. Returned rocket result carries the exact matching launch position; direct/dash/None return zero. No raycast or consumer mutation.
- implementation: change `BallMotor.QueueImpulse(Vector3)` to bool: false on existing disabled/nonfinite/zero gates, true only after queue accumulation; ignored return stays source-compatible. Shotgun records owner touch immediately after and only when ball queue returns true. Explosion resolves source once, preserves player-before-ball dispatch, passes source to ball dispatch, then records touch immediately after accepted nonzero queue. Null/non-roster source moves ball but cannot replace touch. Reset still clears attribution.
- done when: pure tests cover action ordering, visibility, invalid aim, corridor geometry, inclusive ranges, every rocket-jump gate, and stronger-action suppression; existing relationship/damage/knockback/cooldown/immunity contracts remain unchanged; T9 can translate one result into T4/existing request APIs.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Bots/BotCombatRules.cs Assets/_Game/Scripts/Runtime/Ball/BallMotor.cs Assets/_Game/Scripts/Runtime/Weapons/ShotgunWeapon.cs Assets/_Game/Scripts/Runtime/Weapons/ExplosionResolver.cs Assets/_Game/Scripts/Tests/EditMode/BotCombatRulesTests.cs -> clean patch syntax/whitespace for combat core, attribution, and tests; authoritative Unity compile/tests remain T12`
- review_focus: direct consumer mutation, friendly firing, incorrect touch attribution, or combat utility outranking goal defense can break core play and scoring; inspect returned action ordering, visibility, relationship policy, and nonzero impulse conditions.
- review_checkpoint: CP8

### T9: Integrate BotController decision, aim, navigation, and actions

- objective: join frozen rules, sensors, navigation, and consumer seams in sole non-local decision/aim/intent owner.
- covered_requirements: B2-B5 controller integration; D1-D11 decision join
- owner: `luna_max` implementation worker
- dependencies: `JOIN1 -> controller consumes accepted T2-T8 contracts; every producer must be frozen`
- parallel_contract: outputs final concrete `BotController` surface consumed by T10/T11; owns no participant lifecycle
- owns: `Assets/_Game/Scripts/Runtime/Bots/BotController.cs`
- protected: every T1-T8 file; `ParticipantState`; `PlayerInputReader`; `PlayerLook`; match/reset owners; consumer side effects; generated assets
- read_paths: `Assets/_Game/Scripts/Runtime/Bots/BotContracts.cs -> frozen intent/difficulty/decision types`; `Assets/_Game/Scripts/Runtime/Bots/BotNavigator.cs -> steering/vertical-route API`; `Assets/_Game/Scripts/Runtime/Bots/BotPerception.cs,Assets/_Game/Scripts/Runtime/Bots/BotTeamRoleCoordinator.cs -> observation/role APIs`; `Assets/_Game/Scripts/Runtime/Bots/BotCombatRules.cs -> action result`; `Assets/_Game/Scripts/Runtime/Movement/PlayerLook.cs -> mutually exclusive local aim ownership`; `Assets/_Game/Scripts/Runtime/Match/MatchController.cs/ApplyGameplayGate -> upstream gate and difficulty assignment caller`
- validation_environment: source-only isolated worktree after JOIN1; no builder or generated mutation
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: add `[DisallowMultipleComponent,DefaultExecutionOrder(-200)] BotController`. Serialized exact refs: participant, match, motor, Head, kick, launcher, shotgun, navigator, perception, role coordinator, combat obstacle mask; pitch max `89°`; jump probe `4m`. Public: Participant, Difficulty, SimulationEnabled, `bool SetDifficulty(BotDifficulty)`, `void SetSimulationEnabled(bool)`, `void SetPaused(bool)`, `void ResetState()`. `SetPaused` is idempotent and changes only a private nonserialized flag; paused FixedUpdate returns before timers, routing, aim/root/Head transforms, or consumer requests, preserving snapshot/schedules/target/route/aim. Disable/reset/normal simulation false clears paused with existing destructive state clear. Invalid dependency/tuning/difficulty -> exact composition error and disable. Difficulty defaults Medium, refreshes from `match.GetBotDifficulty(participant.Team)` on false->true, persists through reset.
- implementation: FixedUpdate order: perception `-300` -> coordinator `-250` -> controller `-200` -> BallKick `-100` -> ordinary consumers. Private immutable `BotReactedSnapshot(gameplayTime,self,ball,allyA,allyB,enemyA,enemyB,enemyC,pickup0,pickup1,pickup2,pickup3,pickup4,role,ownGoal,enemyGoal)` uses scalar T1 values plus HasData; no collection/live refs. On enable, first reaction after latency+jitter; first decision immediately after first reacted copy. Later reaction/decision timers reschedule independently; ordinal increments only when phase runs. Reaction copies only controller's T7 observer APIs. Decision reads snapshot only.
- implementation: target production uses reacted T7 `OwnGoalPosition/EnemyGoalPosition`; `fieldDirection=normalize(enemy-own)`, lateral=`cross(up,fieldDirection)`, fieldLength=goal distance. Ball estimate at decision uses age plus requested horizon and `GamePhysicsSettings.GravityMagnitude`; grounded snapshot suppresses vertical velocity/gravity and preserves center height; invalid result -> last finite observed position. Emergency `100`: crossing within `0..2.5s`, toward-goal speed >=`0.5`, lateral `±18`, height `0..7`, or close danger progress `0..12`, lateral `22`, height `10`; target `PredictBall(clamp(crossingTime-0.20,0,0.75))`, close danger `PredictBall(0.35)`, unreachable fallback own+forward `12` at nearest lane `-28/0/28`. Defender `90`: assigned Defender -> same coverage; no ball -> lane0. Attacker `70`: assigned Attacker -> `PredictBall(0.50)`. Support `55`: assigned Support+living attacker -> progress `clamp(ballProgress-12,24,fieldLength-24)` and lane opposite attacker lateral; attacker lateral zero -> even support slot +28, odd -28. Pickup candidates use T7. Multiple eligible opponents sort by predicted distance then slot ID. Visible enemy `20`; recent enemy `15`; both within `30m`, action position=`position+velocity*(age+0.25)`. Fallback recent ball `PredictBall(0.35)` score0. No target -> zero move, face enemy goal.
- implementation: probe every candidate through `navigator.TryProbeTarget`; discard unreachable; select via T3 total order. Candidate `expiresAt=min(source observation expiry,nextDecisionAt)`; static role candidate expiry is `nextDecisionAt`; remembered pickup expiry is its next visible contradiction or next decision. Regenerate each decision; preserve key only when reproduced, valid, reachable, and `gameplayTime<=expiresAt`. Gate/path failure between decisions clears motion but does not live-resample. Evaluate selected steering every active step. Root yaw faces finite horizontal stored aim/steering; Head pitch = clamp(`-atan2(y,horizontal)*Rad2Deg`, `±89`). Convert world steering after yaw to clamped local Vector2 dot products.
- implementation: external target knowledge remains reacted; current self-owned Head position, consumer readiness, and navigator route direction are sampled at decision execution. Direct/intercept aim uses T2 once from current Head. For each valid rocket aim, call T4 `TryGetLaunchPosition(head.position,thatDirection,out pose)` once and store separate ball/enemy poses. Call T8 `GetRocketJumpDirection(routeForwardXZ)` once, resolve separate jump pose with the same helper, then run jump probe from that pose. Invalid helper result makes only its candidate ineligible. T1 combat input carries current Head, three cached poses, normalized route XZ. Environment LOS uses reusable `RaycastHit[8]`, triggers ignored, mask excluding Participants/Projectiles; for ball aim ignore colliders belonging to reacted ball Rigidbody, then block on nearest other hit before target distance-.05. Each corridor uses its candidate pose. Call T8 Evaluate once. Translate max one result: Dash->kick; Shotgun->`shotgun.RequestFire(currentHeadPosition,direction)`; Rocket->`launcher.RequestFire(result.LaunchPosition,result.AimDirection)`; RocketJump queues motor jump plus the same exact result pose/direction. Never recompute pose after Evaluate; never call dash/launch/damage/cooldown/impulse owners.
- implementation: `ArmSchedule()` sets first reaction and clears decision readiness. Simulation false/reset/disable clears snapshot, ordinals/timers, target/key, route, stored aim/action, flags. False->true calls ArmSchedule. `ResetState()` clears then calls ArmSchedule when already simulation-enabled; no active bot remains with unarmed timers. Consumer gates/reset clear pending requests. Controller never toggles own enabled state for match/death and never controls local participant.
- done when: controller uses copied knowledge only, deterministic schedules, graph-safe targets, exact aim/probes, one consumer action, and zero local-input overlap.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Bots/BotController.cs -> clean patch syntax/whitespace for sole controller fan-in; authoritative Unity compile remains T12`
- review_focus: live-state bypass, error applied twice, wrong execution order, direct ledge steering, unsafe ray origin, or multiple consumer actions breaks fairness/core play; inspect phase ordering, snapshots, candidate construction, probe calls, pose math, and translation.
- review_checkpoint: CP9

### T10: Integrate participant lifecycle and complete pause semantics

- objective: connect concrete BotController to per-slot gates/resets and finish full no-timescale pause across timer owners.
- covered_requirements: B2/B6 lifecycle integration; pause fidelity
- owner: `luna_max` implementation worker
- dependencies: `CP9 -> ParticipantState serializes concrete accepted controller`; `CP5 -> extends accepted match pause methods`; `CP8 -> extends accepted ShotgunWeapon without conflicting combat writer`; `CP4 -> gates accepted consumer APIs`
- parallel_contract: `None`; sequential fan-in owns participant/match/pause surfaces after prior writers freeze
- owns: `Assets/_Game/Scripts/Runtime/Participants/ParticipantState.cs`; `Assets/_Game/Scripts/Runtime/Match/MatchController.cs`; `Assets/_Game/Scripts/Runtime/Movement/PlayerMotor.cs`; `Assets/_Game/Scripts/Runtime/Movement/PlayerLook.cs`; `Assets/_Game/Scripts/Runtime/Ball/BallKick.cs`; `Assets/_Game/Scripts/Runtime/Ball/BallMotor.cs`; `Assets/_Game/Scripts/Runtime/Pickups/ArenaPickup.cs`; `Assets/_Game/Scripts/Runtime/Weapons/RocketLauncher.cs`; `Assets/_Game/Scripts/Runtime/Weapons/ShotgunWeapon.cs`; `Assets/_Game/Scripts/Runtime/Weapons/RocketProjectile.cs`
- protected: every other T1-T9 file; match score/reset rules; pickup respawn values; weapon combat behavior; Input System; generated assets
- read_paths: `ParticipantState.ApplyLeafSimulation/ResetForKickoff/RespawnAt -> lifecycle owner`; `MatchController.ApplyGameplayGate -> match owner`; `ArenaPickup.FixedUpdate/TryCollect -> respawn and collection`; `ShotgunWeapon.FixedUpdate -> pump timer`; `RocketLauncher.ActiveProjectiles,RocketProjectile collision callbacks -> active rocket freeze`
- validation_environment: source-only isolated worktree after CP9; no builder/generated mutation
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: ParticipantState adds serialized BotController, read-only property, dedicated pause flag, `void SetMatchPaused(bool)`. FixedUpdate returns before immunity/death timers only while paused. `SetMatchPaused` idempotently forwards pause to motor, kick, launcher, shotgun; local slot forwards to PlayerLook, nonlocal slot forwards to BotController; then stores flag. It never changes normal simulation-enabled state. Leaf gate outside pause remains: motor/launcher/kick/shotgun = `matchSimulationEnabled && alive`; input gameplay/look = same + local; bot simulation = same + nonlocal. Component enabled state structural. Reset/respawn order: force bot simulation false -> clear pause through pause APIs -> reset/place leaves -> `botController.ResetState()` -> final `ApplyLeafSimulation`; false->true or reset-while-active re-arms first reaction schedule. Death/freeze uses normal destructive gate; pause never does. Validation requires shared leaf refs+bot on all slots; camera feedback local-only.
- implementation: add idempotent `SetPaused(bool)` to PlayerMotor, PlayerLook, BallKick, BallMotor, RocketLauncher, ShotgunWeapon, RocketProjectile. While paused each Update/FixedUpdate/collision/trigger/request entry returns before mutation. PlayerLook changes only a private nonserialized paused flag and returns at top of Update before cursor consumption, yaw, pitch, root, or Head mutation; MatchHud remains sole pause cursor owner. PlayerMotor preserves velocity, active dash contribution/direction/elapsed/remaining, coyote/jump timers, external impulse, and ground state; pause entry clears only transient device/programmatic move/jump requests. BallKick preserves cooldown and contacted sets; pause entry clears only pending programmatic kick. BallMotor pause owns Rigidbody kinematic freeze/restore using its existing stored velocity/angular pattern but does not clear queued impulses/contact state; QueueImpulse/ApplyKick reject paused. RocketLauncher preserves cooldown and active list, clears only pending fire, forwards pause to every active projectile, and gives newly registered projectile current pause state. Shotgun preserves pump, clears only pending fire. RocketProjectile preserves transform/velocity/lifetime and ignores collision/trigger while paused. Disable/reset clears pause plus existing reset state.
- implementation: exact pause entry after transition validation: set state Paused -> participant slots ascending call `SetMatchPaused(true)` -> `ball.SetPaused(true)` -> invoke `PauseChanged(true)` once. Never call `ApplyGameplayGate(false)` for pause. Resume while still Paused: ball false -> participant slots ascending false -> set state Playing -> reconcile collisions -> invoke `PauseChanged(false)` once. `MatchRules.ShouldAdvancePausedTimer` gates participant immunity/death, pickup respawn, shotgun pump, rocket lifetime. ArenaPickup subscribes match PauseChanged; paused FixedUpdate does not decrement respawn; `TryCollect` requires `match.GameplayEnabled && !paused`. No reset/destroy/score/phase/transform/velocity/cooldown mutation; no pause seam for Setup, GoalFreeze, Reset, countdown, Final; coordinated reset clears stale pause before normal reset. No `Time.timeScale`.
- done when: local slot remains device-only; five nonlocal slots receive bot gates; death/immunity, pickup respawn, pump, ball/player motion, and active rockets freeze only in Paused; GoalFreeze/countdowns preserve existing lifecycle timing.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Scripts/Runtime/Participants/ParticipantState.cs Assets/_Game/Scripts/Runtime/Match/MatchController.cs Assets/_Game/Scripts/Runtime/Movement/PlayerMotor.cs Assets/_Game/Scripts/Runtime/Movement/PlayerLook.cs Assets/_Game/Scripts/Runtime/Ball/BallKick.cs Assets/_Game/Scripts/Runtime/Ball/BallMotor.cs Assets/_Game/Scripts/Runtime/Pickups/ArenaPickup.cs Assets/_Game/Scripts/Runtime/Weapons/RocketLauncher.cs Assets/_Game/Scripts/Runtime/Weapons/ShotgunWeapon.cs Assets/_Game/Scripts/Runtime/Weapons/RocketProjectile.cs -> clean patch syntax/whitespace for lifecycle/pause fan-in; authoritative Unity compile remains T12`
- review_focus: local/bot gate overlap, stale pause flags, partial timer freeze, wrong transition order, or reset side effects can duplicate input or advance state while paused; inspect every entry/exit/reset/death path.
- review_checkpoint: CP10

### T11: Compose and validate authoritative MovementLab source

- objective: author the builder composition and reload validator for every joined bot/navigation/setup contract, with exact generated outputs declared for the later single production wave.
- covered_requirements: builder composition for B1-B6; semantic validation source contract
- owner: `luna_max` implementation worker
- dependencies: `CP10 -> serialized composition/validation requires final reviewed runtime fields, methods, gates, and pause ownership`
- parallel_contract: `None`; this task alone owns builder/validator source and exact output declarations after source integration
- owns: `Assets/_Game/Editor/MovementLab/MovementLabBotPipeline.cs`; `Assets/_Game/Editor/MovementLab/MovementLabContract.cs`; `Assets/_Game/Editor/MovementLab/MovementLabPrefabPipeline.cs`; `Assets/_Game/Editor/MovementLab/MovementLabSceneComposer.cs`; `Assets/_Game/Editor/MovementLab/MovementLabStageGraph.cs`; `Assets/_Game/Editor/MovementLab/MovementLabValidator.cs`; `Assets/_Game/Scripts/Runtime/Bots.meta`; `Assets/_Game/Scripts/Runtime/Bots/*.cs.meta`; `Assets/_Game/Scripts/Tests/EditMode/Bot*Tests.cs.meta`; `Assets/_Game/Editor/MovementLab/MovementLabBotPipeline.cs.meta`
- protected: `MovementLabContractCatalog.cs`; manifest schema `8`; lighting profile values; atomic replacement owner; existing GUIDs/metas; project/package versions; generated outputs until orchestrator-phase trigger
- read_paths: `Assets/_Game/Editor/MovementLabBuilder.cs -> staged facade`; `Assets/_Game/Editor/MovementLab/MovementLabContract.cs/MaterialPrefabOutputs,GameplaySceneOutputs,BakedOutputPaths -> output ownership`; `Tools/Validation/Invoke-MovementLabWorkflow.ps1 -> production ledger and inventory`; `../.agents/skills/orchestrate-implementation/SKILL.md#generated-output-gate -> acceptance contract`
- validation_environment: source-only isolated worktree after CP10; Unity import, generation, and semantic execution deferred exclusively to T12
- unity_mutation: `false`
- expensive_proof_owner: `None`
- expensive_proof_execution: `None`
- implementation: add `MovementLabBotPipeline`. Exact `(id,position,area,safeRadius)`: lattice IDs `0..14`, X-major over X `-52/-12/0/12/52`, Z `-28/0/28`, Y0, Floor, radius4. `15=(12,0,-10)`, `16=(12,0,10)`, `17=(-12,0,10)`, `18=(-12,0,-10)`, Floor radius2. `19=(-36,0,-28)`, `20=(36,0,28)`, `21=(0,0,14)`, `22=(-38,0,18)`, `23=(38,0,-18)`, Floor radius2. West low/high `24/25=RampWest.position + RampWest.rotation*(0,.25,-8/+8)`; east low/high `26/27=RampEast.position + RampEast.rotation*(0,.25,-8/+8)`; all RampDeck radius1.25; validator expected values within `.001`: `24=(-14.207888,.270929,2)`, `25=(-29.662702,4.412034,2)`, `26=(14.207888,.270929,-2)`, `27=(29.662702,4.412034,-2)`. Never use `TransformPoint`. `28=(-34,0,2)`, `29=(34,0,-2)`, Floor radius1.5. `30=(-62,0,0)`, `32=(62,0,0)`, Floor radius2. `31=(-65.5,0,0)`, `33=(65.5,0,0)`, GoalRecess radius.5. Slot0/3 spawns reuse IDs `10/4`. Serialize exact graph controller values `.4/1.8/(0,.9,0)/60/.3/.04`; navigator ground mask1 and exact T6 tuning.
- implementation: exact edge IDs `0..19`: for lattice X columns in ascending X, emit each adjacent-Z pair in ascending Z, forward then reverse. `20..35`: outer lanes Z -28 then +28, emit each adjacent-X pair in ascending X, forward then reverse. `36..39`: `4->7`, `7->4`, `7->10`, `10->7`. `40..55`: `9->15`, `15->9`, `10->15`, `15->10`, `10->16`, `16->10`, `11->16`, `16->11`, `4->17`, `17->4`, `5->17`, `17->5`, `3->18`, `18->3`, `4->18`, `18->4`. `56..75`: pickups 19..23 connect respectively to `{0,3}`, `{11,14}`, `{7,8}`, `{2,5}`, `{9,12}`; emit lower neighbor->pickup, reverse, higher neighbor->pickup, reverse. `76..82`: `4->24`, `24->4` Walk; `24->25`, `25->24` Ramp; `25->28` Drop; `28->1`, `1->28` Walk. `83..89`: `10->26`, `26->10`; `26->27`, `27->26`; `27->29`; `29->13`, `13->29`. `90..93`: `1->30`, `30->1` Walk; `30->31`, `31->30` ShieldGate. `94..97`: `13->32`, `32->13`; `32->33`, `33->32`. Costs Walk/ShieldGate distance, Ramp `1.15*distance`, Drop `1.25*distance`; corridor widths Walk3/Ramp6/Drop2/Shield16. Validate landings and capsule clearance. Enabled production shields make nodes31/33 unreachable.
- implementation: shared Player prefab gets exactly one disabled BotController, BotNavigator, BotPerception; their `OnEnable` owns composition validation, not Awake. ParticipantState back-reference and every same-object field persist. SceneComposer changes the three pickup-build helpers to return their created `ArenaPickup` references, combines exact global order `0 health west,1 health east,2 shotgun,3 ammo west,4 ammo east`, then calls exact editor API `MovementLabBotPipeline.ComposeScene(ParticipantState[] roster,MatchController match,BallMotor ball,ArenaPickup[] pickups,GoalTrigger redDefendedGoal,GoalTrigger blueDefendedGoal,Collider redShield,Collider blueShield)` with NorthGoal as red and SouthGoal as blue. API requires roster length6/pickups length5/non-null inputs or throws exact composition error.
- implementation: `ComposeScene` creates one root-level `BotSystems`; child `NavigationGraph` owns BotNavigationGraph; child `BlueRoleCoordinator` owns BotTeamRoleCoordinator; child `RedRoleCoordinator` owns BotTeamRoleCoordinator. Duplicate root/child/component -> throw. Create both coordinators disabled. Keep every prefab-derived BotController/BotNavigator/BotPerception disabled while writing graph/match/ball/`Head`/roster/pickups/goals/coordinator/masks through SerializedObject and applying without undo. Blue coordinator participant slots `0/1/2`, perception slots `1/2`; Red coordinator participant/perception slots `3/4/5`. Pickup order uses global IDs above. Perception/combat masks exclude Participants+Projectiles. After every serialized value is applied, enable coordinators Blue then Red, then each nonlocal slot in ascending slot order Perception -> Navigator -> Controller; local slot0 remains disabled. Any `OnEnable` failure disables only its owning component and logs its exact dependency error. Preserve exactly one Input reader per player and three HUD serialized refs. Match difficulty remains team-derived through `GetBotDifficulty`.
- implementation: reload validator resolves `BotSystems` from the root GameObject set by exact name, then exact children `NavigationGraph`, `BlueRoleCoordinator`, `RedRoleCoordinator`; never runtime-searches. It checks nonzero fileIDs, prefab provenance, one component set per participant, local-disabled/five-enabled state, both coordinators enabled, same-object back-references, execution orders `-300/-250/-200/-100`, exact pickup order, node IDs `0..33`, edge IDs `0..97`, gate colliders, no reverse drops, ramp/drop/ledge tuning, production recesses unreachable, ordered per-observer sensors/coordinators, HUD enum/policy, six readers sharing input asset, T10 pause adapters, no scene-added replacements. Node clearance uses query radius `controllerRadius-skin=.36`, world center `node.position+(0,.9,0)`, endpoints `center +/- up*(height*.5-controllerRadius)=center +/- up*.5`, Default mask, triggers ignored, fixed buffer32; overflow or any overlap fails. Original `.4` endpoint offset plus `.36` query radius leaves `.04` support gap, so floor/ramp support is not returned. Validator proves persisted structure only; pure tests prove rules/timers; user playtest proves physical behavior.
- implementation: exact MaterialPrefab repository-input additions: `MovementLabBotPipeline.cs`, `BotContracts.cs`, `BotNavigationGraph.cs`, `BotNavigator.cs`, `BotPerception.cs`, `BotTeamRoleCoordinator.cs`, `BotController.cs`, changed `ParticipantState.cs`, `PlayerMotor.cs`, `PlayerLook.cs`, `BallKick.cs`, `RocketLauncher.cs`, `ShotgunWeapon.cs`. Exact GameplayScene additions: same list plus `BotNavigationRules.cs`, changed `MatchController.cs`, `ArenaPickup.cs`, `RocketProjectile.cs`, `BallMotor.cs`, `ExplosionResolver.cs`. Add paired metas through existing `WithMetas` helper. Pure rule/test files not serialized and do not enter stage fingerprints. `MovementLabBuildManifest.json` remains manifest-store/orchestration inventory only; never add it to `MaterialPrefabOutputs`, `GameplaySceneOutputs`, or a stage input/output digest. Create stable metas for every new source/test; bump only Decisions contract versions; no schema/catalog change.
- implementation: exact MaterialPrefab-produced asset pairs declared for T12 ownership are `{Assets/_Game/Prefabs/Player.prefab, Assets/_Game/Prefabs/Ball.prefab, Assets/_Game/Prefabs/Rocket.prefab, Assets/_Game/Prefabs/ExplosionVfx.prefab, Assets/_Game/Prefabs/HealthPickup.prefab, Assets/_Game/Prefabs/ShotgunPickup.prefab, Assets/_Game/Prefabs/AmmoPickup.prefab, Assets/_Game/Animations/WorldCharacter.controller, Assets/_Game/Animations/FpsKick.controller, Assets/_Game/Materials/Floor.mat, Assets/_Game/Materials/Wall.mat, Assets/_Game/Materials/Trim.mat, Assets/_Game/Materials/Hazard.mat, Assets/_Game/Materials/Marking.mat, Assets/_Game/Materials/Ball.mat, Assets/_Game/Materials/Rocket.mat, Assets/_Game/Materials/RocketHot.mat, Assets/_Game/Materials/ProjectileGlow.mat, Assets/_Game/Materials/GoalFrame.mat, Assets/_Game/Materials/Shield.mat, Assets/_Game/Materials/ShieldBlue.mat, Assets/_Game/Materials/ShieldRed.mat, Assets/_Game/Materials/ArenaPrimary.mat, Assets/_Game/Materials/ArenaTrim.mat, Assets/_Game/Materials/ArenaHazard.mat, Assets/_Game/Materials/ArenaGlow.mat, Assets/_Game/Materials/BallSurface.physicMaterial, Assets/_Game/Materials/Explosion.mat, Assets/_Game/Materials/ExplosionAdditive.mat, Assets/_Game/Materials/ExplosionSparks.mat, Assets/_Game/Materials/Smoke.mat, Assets/_Game/Materials/ContainmentGridCeiling.mat, Assets/_Game/Materials/ContainmentGridLongWall.mat, Assets/_Game/Materials/ContainmentGridEndWall.mat, Assets/_Game/Materials/RetroSunnySky.mat, Assets/_Game/Materials/CharacterRed.mat, Assets/_Game/Materials/CharacterBlack.mat, Assets/_Game/Materials/CharacterCream.mat, Assets/_Game/Materials/CharacterEye.mat, Assets/_Game/Materials/WeaponMetal.mat, Assets/_Game/Materials/WeaponDark.mat, Assets/_Game/Materials/WeaponAccent.mat, Assets/_Game/Materials/ShotgunMetal.mat, Assets/_Game/Materials/ShotgunDark.mat, Assets/_Game/Materials/ShotgunAccent.mat, Assets/_Game/Materials/TeamBlue.mat, Assets/_Game/Materials/TeamRed.mat, Assets/_Game/Materials/TeamBlueShield.mat, Assets/_Game/Materials/TeamRedShield.mat, Assets/_Game/Materials/TeamBlueTrail.mat, Assets/_Game/Materials/TeamRedTrail.mat, Assets/_Game/Materials/HealthPickup.mat, Assets/_Game/Materials/AmmoShell.mat, Assets/_Game/Generated/BlueCircleCueMesh.asset, Assets/_Game/Generated/RedTriangleCueMesh.asset}`; literal `<asset>.meta` pair also T12-owned.
- implementation: exact GameplayScene/manifest outputs declared for T12 ownership are `{Assets/_Game/Scenes/MovementLab.unity, Assets/_Game/Generated/MovementLabBuildManifest.json}` with literal `.meta` pairs, plus `{ProjectSettings/EditorBuildSettings.asset, ProjectSettings/DynamicsManager.asset, ProjectSettings/TimeManager.asset, ProjectSettings/TagManager.asset}`. Exact T12 production-bake outputs are `{Assets/_Game/Lighting/MovementLabLightingSettings.asset, Assets/_Game/Lighting/MovementLabVolumeProfile.asset, Assets/_Game/Lighting/MovementLabLightingManifest.json, Assets/_Game/Scenes/MovementLab/LightingData.asset, Assets/_Game/Scenes/MovementLab/Lightmap-0_comp_dir.png, Assets/_Game/Scenes/MovementLab/Lightmap-0_comp_light.exr, Assets/_Game/Scenes/MovementLab/Lightmap-0_comp_shadowmask.png, Assets/_Game/Scenes/MovementLab/Lightmap-1_comp_dir.png, Assets/_Game/Scenes/MovementLab/Lightmap-1_comp_light.exr, Assets/_Game/Scenes/MovementLab/Lightmap-1_comp_shadowmask.png, Assets/_Game/Scenes/MovementLab/Lightmap-2_comp_dir.png, Assets/_Game/Scenes/MovementLab/Lightmap-2_comp_light.exr, Assets/_Game/Scenes/MovementLab/Lightmap-2_comp_shadowmask.png, Assets/_Game/Scenes/MovementLab/Lightmap-3_comp_dir.png, Assets/_Game/Scenes/MovementLab/Lightmap-3_comp_light.exr, Assets/_Game/Scenes/MovementLab/Lightmap-3_comp_shadowmask.png, Assets/_Game/Scenes/MovementLab/Lightmap-4_comp_dir.png, Assets/_Game/Scenes/MovementLab/Lightmap-4_comp_light.exr, Assets/_Game/Scenes/MovementLab/Lightmap-4_comp_shadowmask.png, Assets/_Game/Scenes/MovementLab/ReflectionProbe-0.exr, Assets/_Game/Scenes/MovementLab/ReflectionProbe-1.exr, Assets/_Game/Scenes/MovementLab/ReflectionProbe-2.exr, Assets/_Game/Scenes/MovementLab/ReflectionProbe-3.exr}` and every literal paired `.meta`.
- done when: builder and reload-validator source symmetrically cover the shared prefab, six participants, graph, coordinators, setup/pause, exact stage inputs, contract version bumps, and every declared output without importing or mutating the project.
- checks: `proof: git diff --check <dispatch_snapshot_sha> -- Assets/_Game/Editor/MovementLab/MovementLabBotPipeline.cs Assets/_Game/Editor/MovementLab/MovementLabContract.cs Assets/_Game/Editor/MovementLab/MovementLabPrefabPipeline.cs Assets/_Game/Editor/MovementLab/MovementLabSceneComposer.cs Assets/_Game/Editor/MovementLab/MovementLabStageGraph.cs Assets/_Game/Editor/MovementLab/MovementLabValidator.cs -> clean patch syntax/whitespace for builder/validator source; authoritative generation/validation remain T12`
- review_focus: incomplete stage registration, builder/validator asymmetry, wrong graph identity, or missing persisted-reference assertions can make source appear complete while reload fails; inspect exact source/output declarations, contract bumps, graph geometry derivation, and no catalog/schema change.
- review_checkpoint: CP11

### T12: Generate and classify authoritative outputs

- objective: after every source checkpoint and accepted fix, run the sole authorized production preparation/build and return exact generated-output evidence through a standard writer barrier.
- covered_requirements: final automated validation boundary for B1-B6 and D1-D11
- owner: `luna_max` generation worker
- dependencies: `CP11 + committed T1-T11 source fan-in + accepted checkpoint fixes -> one bounded writer receives exact sourceFreezeSha, generated inventory, Unity command, and evidence paths`
- parallel_contract: `None`; shared Unity/project mutation, generated outputs, private Library, and evidence ledger require one sequential writer; final tests/validator wait for CP12
- owns: T11 exact MaterialPrefab, GameplayScene/manifest, production-bake, ProjectSettings, and literal paired `.meta` outputs; task-local workflow ledger/evidence; no source files
- protected: all T1-T11 frozen source; `MovementLabContractCatalog.cs`; manifest schema `8`; lighting values; atomic replacement owner; existing GUIDs/metas; project/package versions outside declared outputs
- read_paths: `Assets/_Game/Editor/MovementLabBuilder.cs -> staged facade`; `Tools/Validation/Invoke-MovementLabWorkflow.ps1 -> production prepare/validate ledger`; `Tools/Validation/Compare-GeneratedYaml.ps1 -> generated-output acceptance`; `../.agents/skills/orchestrate-implementation/SKILL.md#generated-output-gate -> authoritative changed-output contract`
- validation_environment: one Unity Editor lease on a short `C:\wt\<id>` worktree with private preserved `Library`; short evidence alias `C:\wt\<id>e`; no interactive Editor/project lock; all Unity commands use the same short project path; harness passes before mutation
- unity_mutation: `true`
- expensive_proof_owner: T12 generation worker
- expensive_proof_execution: `same_dispatch`
- implementation: orchestrator freezes/commits accepted T1-T11 sources as `sourceFreezeSha`, captures scoped-clean status, then dispatches one T12 writer with unique execution ID and exact commands. Worker runs harness, ProductionPrepare once for skip-or-one-bake plus authoritative build, classifies exact `workflow-result.changedGeneratedPaths` against T11 inventory, runs comparator `-Base sourceFreezeSha -Head WORKTREE -Path <exact changed array> -FailOnDangling`, and writes raw output at `C:\wt\<id>e\generated-comparator-<attempt>.txt`. Worker performs no Git/state/plan writes and returns standard Worker Result with exact changed paths, checks, workflow result/checkLedger paths, comparator path/digest, lock proof, and unowned churn.
- implementation: orchestrator barrier parses comparator output with anchored invariant lines: one `COVERAGE: <reported>/<selected> authoritative changed paths reported; semantic checked <checked>; NOT CHECKED <notChecked>`; one `SEMANTIC: changed|identical`; one `DANGLING: <head> (base <base>)`; one `GUID: stable <stable>; churn <churn>; added <added>; removed <removed>; invalid <invalid>`; one `PAIRS: intact <intact>; broken <broken>`; one `UNSUPPORTED: <count>`; one exact `== <path>` block per changed path and no extra block. Missing/duplicate/malformed summary fails. Require reported=selected=changed-path count, checked+notChecked=selected, head dangling <= base, GUID churn/removed/invalid0, broken pairs0, unsupported0. Every NOT CHECKED block must have kind exactly `metadata` or `binary provenance`; metadata requires presence+pair and GUID line when applicable; binary requires presence+bytes+blob+provenance+pair. A block with dangling+instances+canonical and no explicit kind is `unity-yaml`; supported/non-YAML/unity-YAML text must count semantically checked; unsupported kind always fails. Declared semantic change is allowed.
- implementation: after valid worker return, orchestrator closes writer barrier, captures ordinal-sorted worktree blob IDs using Git-filter-aware `git hash-object --path=<path> <path>`, commits only exact generated churn as `chore: regenerate MovementLab outputs`, captures matching commit-tree IDs, then atomically writes absent `C:\wt\<id>e\generated-transition-<attempt>.json` through same-parent unique temp + `Move-Item`. Schema1 exact JSON: `schemaVersion:1`; 40-lower-hex `sourceFreezeSha`; `comparatorBaseSha` equal sourceFreezeSha; `comparatorHead:"WORKTREE"`; ordinal-sorted unique `changedPaths:string[]`; `comparatorOutputPath:string`; 64-lower-hex `comparatorSha256`; `coverageSummary:{reportedCount:int,selectedCount:int,checkedCount:int,notCheckedCount:int}`; `pathCoverage:[{path:string,kind:"unity-yaml"|"supported text"|"non-yaml text"|"metadata"|"binary provenance",semanticChecked:bool}]`; `semanticSummary:"changed"|"identical"`; `danglingSummary:{headCount:int,baseCount:int}`; `guidSummary:{stableCount:int,churnCount:int,addedCount:int,removedCount:int,invalidCount:int}`; `pairSummary:{intactCount:int,brokenCount:int}`; `unsupportedCount:int`; ordinal-sorted `worktreeBlobs:[{path:string,oid:40-lower-hex}]`; 40-lower-hex `generatedCommitSha`; ordinal-sorted `commitBlobs:[{path:string,oid:40-lower-hex}]`. Require changed/header/pathCoverage/blob order equality, worktree/commit OID equality, numeric/block predicates above, output SHA match, generated commit parent=sourceFreezeSha, and commit diff paths exactly changedPaths.
- done when: T12 worker returns accepted production result and comparator evidence; orchestrator creates exact separate generated commit and valid immutable transition; CP12 can bind one real writer execution ID to `sourceFreezeSha..generatedCommitSha`.
- checks: `proof: powershell -NoProfile -ExecutionPolicy Bypass -File Tools/Tests/Invoke-HarnessTests.ps1 -> pass under 90 seconds immediately before ProductionPrepare, with no Editor/project lock`
- checks: same-dispatch trigger is `CP11 accepted + T1-T11 source fan-in committed + all checkpoint fixes accepted + scoped-clean status`; declared producers are T1-T11; declared outputs are exact T11 MaterialPrefab, GameplayScene/manifest, and production-bake paths.
- checks: `check_id=production-bake; tier=production-final; owner=workflow-orchestrator; expected_status=reused iff one exact source-defined skip marker and bakeCount=0, otherwise executed iff marker absent and bakeCount=1; command=powershell -NoProfile -ExecutionPolicy Bypass -File Tools/Validation/Invoke-MovementLabWorkflow.ps1 -Mode ProductionPrepare -ProjectPath C:\wt\<id> -EvidenceRoot C:\wt\<id>e -AttemptId <attempt>; mutates_project=true; input_paths=[Assets/_Game/Lighting, Assets/_Game/Editor/MovementLab/MovementLabLightingPipeline.cs, Assets/_Game/Editor/MovementLab/MovementLabLightingProfiles.cs, Assets/_Game/Lighting/MovementLabLightingSettings.asset, Assets/_Game/Lighting/MovementLabLightingSettings.asset.meta, Assets/_Game/Lighting/MovementLabLightingSettings_Development.asset, Assets/_Game/Lighting/MovementLabLightingSettings_Development.asset.meta, Assets/_Game/Lighting/MovementLabVolumeProfile.asset, Assets/_Game/Lighting/MovementLabVolumeProfile.asset.meta, Assets/_Game/Lighting/MovementLabLightingManifest.json, Assets/_Game/Lighting/MovementLabLightingManifest.json.meta]; input_digest=workflow SHA-256; environment_fingerprint=workflow-reported Windows/Unity/private-Library fingerprint; invalidation_paths=[same exact input_paths]; subsumes=[]; run_point=source-freeze after CP11/fixes; evidence=workflow result complete, exact conditional row status, exactSha=sourceFreezeSha, marker/bakeCount contract, changedGeneratedPaths, evidence digest, lock release`
- checks: `proof: powershell -NoProfile -ExecutionPolicy Bypass -File Tools/Validation/Compare-GeneratedYaml.ps1 -Base <sourceFreezeSha> -Head WORKTREE -Path <workflow.changedGeneratedPaths> -FailOnDangling -> exact selected/header coverage, SEMANTIC evidence, no DANGLING increase, stable existing GUIDs, intact Assets pairs, ProjectSettings pair not-applicable, UNSUPPORTED 0, binary provenance; schema1 transition file binds worktree blobs to generated commit tree`
- review_focus: unclassified churn, malformed comparator parsing, stale transition evidence, or build evidence from another SHA can falsely accept output. CP12 standard reviewer contract covers T12 writer execution ID, task/generated path slice, `review_base_sha=sourceFreezeSha`, `frozen_sha=generatedCommitSha`, mode `separate-commit`, and attaches ProductionPrepare result/finalized checkLedger, comparator output/digest, exact coverage classification, and schema1 transition. Raw binaries stay provenance-only.
- review_checkpoint: CP12; fresh `sol_medium` standard reviewer after orchestrator commits the verified T12 worker slice; Critical/High finding reopens owning source checkpoint and every invalidated generation/final row

## Execution Assignments

- workers: START fan-out -> T1 `luna_max` shared contracts || T4 `luna_max` consumer APIs. CP1 fan-out -> T2 `luna_max` difficulty/aim || T3 `luna_max` role/target || T5 `luna_max` setup/pause. CP4 -> T6 `luna_max` navigation. CP1+CP3+CP5 -> T7 `luna_max` perception/roles. CP1+CP2+CP4 -> T8 `luna_max` combat/attribution. Ready path-disjoint lanes run in parallel. JOIN1 -> T9 `luna_max` BotController. Then T10 `luna_max` participant/pause fan-in -> T11 `luna_max` builder/validator -> T12 `luna_max` generation worker. Execution orchestrator alone freezes/commits source, commits verified generated slice, writes transition, and runs post-CP12 final proof.
- review_checkpoints: CP1 -> T1, no dependency, fresh `sol_medium`; CP2 -> T2 after CP1, fresh `sol_medium`; CP3 -> T3 after CP1, fresh `sol_medium`; CP4 -> T4, no dependency, fresh `sol_medium`; CP5 -> T5 after CP1, fresh `sol_medium`; CP6 -> T6 after CP4, fresh `sol_medium`; CP7 -> T7 after CP1+CP3+CP5, fresh `sol_medium`; CP8 -> T8 after CP1+CP2+CP4, fresh `sol_medium`; JOIN1 waits CP2-CP8 accepted and zero producer writers; CP9 -> T9 after JOIN1, fresh `sol_medium`; CP10 -> T10 after CP4+CP5+CP8+CP9, fresh `sol_medium`; CP11 -> T11 after CP10, fresh `sol_medium`; CP12 -> T12 writer execution ID and separate generated commit after T12 barrier, fresh `sol_medium`. Review/fix/re-review follows [`orchestrate-implementation`](../.agents/skills/orchestrate-implementation/SKILL.md#review-checkpoints); every worker has a standard checkpoint with a concrete execution ID.

## Final Verification

- exact head: require a clean regenerated SHA descended from `c275c092ff0a042241034a062fe5a4beb389a4a4`, with accepted source commits plus a separate `chore: regenerate MovementLab outputs` commit; tests and ProductionValidate bind this final SHA, ProductionPrepare retains its exact source-freeze ledger provenance, and separate immutable transition evidence binds the accepted comparator/worktree hashes to the final generated commit tree; the pinned plan hash/size must still match.
- checks: exact order after CP11: T12 harness -> ProductionPrepare production-final row -> generated gate -> T12 return -> orchestrator separate generated commit/transition -> CP12 -> harness -> EditMode tests -> clean-SHA check -> harness -> ProductionValidate production-final row. ProductionPrepare is sole production bake/authoritative build. No earlier bake or duplicate build/compile. Classify by exit code, typed workflow result/ledger, Unity exceptions/compile errors, semantic result, and lock proof; never keyword-only log scan.
- checks: `proof: powershell -NoProfile -ExecutionPolicy Bypass -File Tools/Tests/Invoke-HarnessTests.ps1 -> pass under 90 seconds immediately after CP12 and before EditMode Unity invocation`
- checks: `proof: powershell -NoProfile -Command '& { $unityPath="C:\Program Files\Unity\Hub\Editor\6000.5.6f1\Editor\Unity.exe"; $unityArgs=@("-batchmode","-nographics","-projectPath","C:\wt\<id>","-runTests","-testPlatform","EditMode","-testResults","C:\wt\<id>e\editmode-results.xml","-logFile","C:\wt\<id>e\unity-editmode.log"); $unityProcess=Start-Process -FilePath $unityPath -ArgumentList $unityArgs -Wait -PassThru -WindowStyle Hidden; exit $unityProcess.ExitCode }' -> exit0, relevant NUnit XML zero failures, zero compile/Console errors, lock release at generated commit SHA`
- checks: after EditMode Unity release, remove only newly generated root IDE files allowed by repository policy; require `git rev-parse HEAD` equals generatedCommitSha and scoped `git status --porcelain` is clean. Any other mutation reopens affected generated acceptance/review/tests.
- checks: `proof: powershell -NoProfile -ExecutionPolicy Bypass -File Tools/Tests/Invoke-HarnessTests.ps1 -> pass under 90 seconds immediately before ProductionValidate workflow invocation`
- checks: `check_id=production-validator; tier=production-final; owner=workflow-orchestrator; expected_status=executed; command=powershell -NoProfile -ExecutionPolicy Bypass -File Tools/Validation/Invoke-MovementLabWorkflow.ps1 -Mode ProductionValidate -ProjectPath C:\wt\<id> -EvidenceRoot C:\wt\<id>e -AttemptId <attempt>-validate -LedgerPath <production-prepare-checkLedgerPath>; mutates_project=false; input_paths=[Assets/_Game/Editor, Assets/_Game/Scripts/Runtime, Assets/_Game/Generated, Tools/Validation, Packages, ProjectSettings]; input_digest=workflow SHA-256; environment_fingerprint=workflow-reported Windows/Unity/private-Library fingerprint; invalidation_paths=[Assets/_Game/Editor, Assets/_Game/Scripts/Runtime, Assets/_Game/Generated, Tools/Validation, Packages, ProjectSettings, Assets/_Game/Lighting, Assets/_Game/Scenes]; subsumes=[validator-readonly]; run_point=final after CP12 and EditMode clean-SHA recheck; evidence=workflow result complete, row executed, exactSha=generatedCommitSha, finalized prepare checkLedger retained as history, current production probe, semantic validator success, evidence digest, lock release`
- inspect: `git diff <start_sha>..<final_sha>` contains only owned source, new source `.meta` pairs, exact declared generated output, and the separate regeneration commit; all expected Bot sources/tests exist; all changed authoritative outputs have exact comparator evidence; GUIDs are stable and every asset/meta pair is intact; render budgets remain report-only.
- invalidation: any accepted fix after a row intersects that row's `invalidation_paths` and reruns it. Any editor/runtime/source change after production preparation reopens generated acceptance and applicable tests/validator; any lighting-input change reopens the production-bake gate. Do not reuse evidence across a different SHA.
- manual post-handoff: user runs one five-minute 3v3 playtest after automated completion, checking five bots move/aim/navigate ramps, contest/defend/score, use rockets/dash/shotgun, show enemy knockback, honor Low/Medium/High setup and pause display. No implementation worker waits on this playtest.

## Handoff

- residual risks: feel/tuning of reaction, aim, role hysteresis, pickup etiquette, enemy-rocket knockback, dash decisions, and High rocket jump remain human playtest findings.
- authority: implementation remains on the isolated plan branch. Integration into the user branch and any follow-up tuning require explicit user/LP approval; the execution orchestrator has no merge authority.
