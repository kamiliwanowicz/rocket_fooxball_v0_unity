interface:
  display_name: "Loop Orchestrator"
  short_description: "Coordinate isolated implementation, review, fixes, and integration"
  default_prompt: "Use $loop-orchestrator for bounded delegated work with exact-SHA review and verified handoff."

policy:
  allow_implicit_invocation: false
